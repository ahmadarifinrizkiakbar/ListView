<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.extrapedia">

    <application
        android:allowBackup="true"
        android:icon="@drawable/laucher"
        android:label="AnimaApp"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ExtraPedia">
        <activity android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="com.example.extrapedia.Profile"
            android:parentActivityName=".MainActivity" />
        <activity android:name="com.example.extrapedia.DaftarHewanActivity"
            android:parentActivityName=".MainActivity"/>
        <activity android:name="com.example.extrapedia.ProfileHewan" />


    </application>


</manifest>